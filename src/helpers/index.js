/**
 * Don't know if you will need them
 * but put one off functions to setup things in here.
 * 
 * E.g. passport if you end up using auth
 * E.g. a logger if you want to use something like winston
 * E.g. Other express middleware (morgan, compression, body-parser etc)
 */
import routing from './routing';

export default {
    routing
};